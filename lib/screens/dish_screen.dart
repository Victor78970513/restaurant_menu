import 'package:flutter/material.dart';

class DishScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DishScreen'),
      ),
    );
  }
}
